# Secrets — never commit
.env
.env.local
.env.*.local

node_modules
dist

*.log
.DS_Store
Thumbs.db

# OpenAI-generated marketplace supply PNGs (run npm run generate:supply-images)
public/supplies/*.png
public/supplies/*.webp

# Peer listing uploads (local dev)
public/listing-uploads/
